# Application
APP_NAME=Koschei Web3 Hub
PORT=10000
APP_ENV=production
STATIC_DIR=public
PUBLIC_APP_URL=https://tradepigloball.co
CORS_ORIGIN=https://tradepigloball.co,https://www.tradepigloball.co

# Required production security
# Generate strong, independent random values. Do not reuse database or provider credentials.
API_KEY_PEPPER=
USER_SESSION_SECRET=

# Neon Postgres
# Use the connection URL supported by the deployed driver and Neon configuration.
DATABASE_URL=
DATABASE_READ_URL=
# Cost-safe connection-pool defaults for the production API.
DB_MAX_OPEN_CONNS=5
DB_MAX_IDLE_CONNS=0
DB_CONN_MAX_LIFETIME_SECONDS=300
DB_CONN_MAX_IDLE_TIME_SECONDS=60
DB_APPLICATION_NAME=koschei-api
# Direct connection is the default. Set 1 only when pooler behavior is intentionally required and tested.
DATABASE_URL_ALLOW_POOLER=0

# Neon Auth
NEON_AUTH_BASE_URL=https://YOUR_NEON_AUTH_HOST/neondb/auth
EXPO_PUBLIC_NEON_AUTH_URL=https://YOUR_NEON_AUTH_HOST/neondb/auth
NEON_AUTH_ISSUER=https://YOUR_NEON_AUTH_HOST
NEON_AUTH_JWKS_URL=https://YOUR_NEON_AUTH_HOST/neondb/auth/.well-known/jwks.json
NEON_AUTH_STATE_SECRET=

# Owner access
OWNER_SECRET=
OWNER_WALLET=

# Legacy owner aliases; do not use for new installations.
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Recovered runtime control plane
# These switches are live runtime policy, not documentation-only aliases.
FEATURE_SOLANA=true
FEATURE_RISK_SCANNER=true
FEATURE_LAUNCH_PAGE_BUILDER=true
# Public badge stays opt-in until it is backed by the canonical unified decision path.
KOSCHEI_PUBLIC_BADGE_ENABLED=false
# Empty KOSCHEI_SECURITY_MODULES enables the complete registered module set.
# Otherwise provide comma-separated canonical module IDs, e.g. holder_concentration,repeat_actor_scan.
KOSCHEI_SECURITY_MODULES=
KOSCHEI_SECURITY_PROVIDER=auto
# evidence_first | strict | observe | evidence_only
KOSCHEI_VERDICT_MODE=evidence_first
# Final-verdict authentication. Configure both values together.
# The private key accepts a base64/base64url/hex Ed25519 32-byte seed or canonical 64-byte private key.
# Never commit production signing material. When unset, verdicts remain deterministic but signed=false.
KOSCHEI_VERDICT_SIGNING_KEY_ID=
KOSCHEI_VERDICT_SIGNING_PRIVATE_KEY=
SOLANA_NETWORK=solana-mainnet
# auto | alchemy | helius | quicknode | rpc | solscan
WEB3_PROVIDER=auto
# Bounded Cargo/LiteSVM build concurrency [1,64].
WORKER_MAX_BUILD_THREADS=2

# Canonical Solana RPC
# ARVIS and Transaction Guard resolve one canonical mainnet endpoint.
# Resolution priority:
# SOLANA_RPC_URL -> provider URL -> ALCHEMY_API_KEY -> public mainnet fallback.
SOLANA_RPC_URL=
ALCHEMY_SOLANA_RPC_URL=
HELIUS_SOLANA_RPC_URL=
QUICKNODE_SOLANA_RPC_URL=
ALCHEMY_API_KEY=
# Optional Helius REST/DAS key. Keep secret; never expose to browser code.
HELIUS_API_KEY=
SOLANA_WSS_URL=
ALCHEMY_SOLANA_WSS_URL=
HELIUS_SOLANA_WSS_URL=
QUICKNODE_SOLANA_WSS_URL=
# Quota protection: production defaults to saver mode in code.
# Manual Safe Check, token scans and user-triggered reports stay live; background firehose workers pause unless forced.
SOLANA_RPC_LIMIT_SAVER_ENABLED=true
SOLANA_RPC_MIN_INTERVAL_MS=1500
SOLANA_RPC_MAX_429_RETRIES=0
SOLANA_RPC_429_BACKOFF_MS=2000
SOLANA_RPC_429_COOLDOWN_SECONDS=600
SOLANA_RPC_BUDGET_ENABLED=true
SOLANA_RPC_BUDGET_MAX_REQUESTS=220
SOLANA_RPC_BUDGET_WINDOW_SECONDS=3600
# JSON-RPC transaction-batch safety. Set false as an emergency provider-compatibility kill switch.
SOLANA_RPC_BATCH_ENABLED=true
SOLANA_RPC_TX_BATCH_SIZE=8
SOLANA_RPC_BATCH_COOLDOWN_SECONDS=600

# Helius provider-cost gates.
# Wallet Identity/batch-identity and getTransactionsForAddress require paid Helius plans.
# Keep these false on the Free plan; standard Solana RPC and supported Free Wallet API endpoints remain available.
KOSCHEI_HELIUS_ENHANCED_HISTORY_ENABLED=false
HELIUS_CREATED_MINT_ARCHIVAL_ENABLED=false
HELIUS_WALLET_IDENTITY_ENABLED=false
# Provider-portable fallback bounds used while Helius archival created-mint history is disabled.
CREATED_MINT_RPC_SIGNATURE_LIMIT=100
CREATED_MINT_RPC_TRANSACTION_LIMIT=40

# ARVIS live radar
# Keep background scanning off on small provider quotas. Turn it on only after upgrading RPC capacity.
KOSCHEI_AUTOMATIC_SCANNING_ENABLED=false
# Explicit owner test mode: starts automatic workers and removes Koschei's local
# RPC and per-cycle report caps. Provider-side rate limits still apply. Off by default.
KOSCHEI_OWNER_UNLIMITED_AUTOSCAN_ENABLED=false
KOSCHEI_AUTO_RADAR_ENABLED=0
KOSCHEI_SOLANA_WATCH_MODE=manual
RADAR_STREAM_ENABLED=0
KOSCHEI_AUTO_RADAR_FORCE_BACKGROUND=false
RADAR_STREAM_FORCE_ENABLED=false
ARVIS_BACKGROUND_RPC_ENRICHMENT_ENABLED=false
ARVIS_HEARTBEAT_DISABLED=true
ARVIS_STREAM_VERDICT_DISABLED=true
ARVIS_HEARTBEAT_SECONDS=900
ARVIS_STREAM_VERDICT_SECONDS=300
KOSCHEI_RADAR_POLL_SECONDS=900
RAYDIUM_PROGRAM_ID=675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8
PUMP_FUN_PROGRAM_ID=6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P

# Selective automatic Pump Radar. Free PumpPortal discovery stays live in RPC saver mode;
# only mints whose aggregated 24h USD pair volume crosses the threshold receive full ARVIS RPC enrichment.
PUMPPORTAL_ENABLED=true
PUMPPORTAL_DATA_WS=wss://pumpportal.fun/api/data
PUMPPORTAL_API_KEY=
PUMP_HIGH_VOLUME_RADAR_ENABLED=true
PUMP_HIGH_VOLUME_MIN_24H_USD=500000
PUMP_HIGH_VOLUME_POLL_SECONDS=300
PUMP_HIGH_VOLUME_REPORT_COOLDOWN_SECONDS=21600
PUMP_HIGH_VOLUME_ATTEMPT_COOLDOWN_SECONDS=1800
PUMP_HIGH_VOLUME_CANDIDATE_PAGE_SIZE=900
PUMP_HIGH_VOLUME_MAX_REPORTS_PER_CYCLE=1

# Optional AI explanation layer
# AI explanations do not override evidence or signed verdicts.
AI_ENABLED=true
# auto | together
AI_PROVIDER=auto
KOSCHEI_MODEL_ROUTER_ENABLED=true
TOGETHER_API_KEY=
TOGETHER_MODEL=
TOGETHER_AI_ENABLED=true
# Optional Solscan Pro capability/usage probe. Indexed Solscan data is never
# promoted to VERIFIED evidence without canonical RPC confirmation.
SOLSCAN_API_KEY=

# Cache and jobs
CACHE_ENABLED=true
CACHE_KEY_PREFIX=koschei
REDIS_URL=
REDIS_TLS=false
NATS_URL=
NATS_SUBJECT_PREFIX=koschei.web3
WEB3_WORKER_ENABLED=false
WEB3_WORKER_CONCURRENCY=8
WEB3_JOB_TIMEOUT_SECONDS=60
WEB3_JOB_MAX_ATTEMPTS=3

# Jito protected send
JITO_BUNDLE_URL=https://mainnet.block-engine.jito.wtf/api/v1/bundles
JITO_AUTH_TOKEN=

# Unified security alerts
# Telegram delivery uses a Bot API token; the legacy TELEGRAM_WEBHOOK_URL remains for Liquidity Radar compatibility.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_WEBHOOK_URL=
# Must be an official https://discord.com/api/webhooks/... endpoint.
DISCORD_WEBHOOK_URL=
# Only high and critical events reach system Telegram/Discord channels by default.
SECURITY_ALERT_MIN_SEVERITY=high
WHITEHAT_ALERT_ADDRESSES=

# Transaction Guard v2
KOSCHEI_TRANSACTION_FIREWALL_ENABLED=true
# Optional comma-separated operator denylist. Request-supplied policy IDs are validated strictly by the API.
TRANSACTION_GUARD_BLOCKED_PROGRAMS=
# Signed enforcement permit control plane. The private key accepts a base64/base64url/hex
# Ed25519 32-byte seed or 64-byte private key. Never commit a production key.
TRANSACTION_GUARD_ENFORCEMENT_KEY_ID=
TRANSACTION_GUARD_ENFORCEMENT_PRIVATE_KEY=
# Clamped to 10..600 seconds.
TRANSACTION_GUARD_ENFORCEMENT_PERMIT_TTL_SECONDS=90
# true = fail closed when an otherwise-ALLOW decision cannot receive a signed permit.
TRANSACTION_GUARD_REQUIRE_ENFORCEMENT_PERMIT=false

# Legacy compatibility aliases; do not use for new installations.
CORS_ALLOWED_ORIGIN=

# Pay-per-tool foundation remains disabled unless explicitly enabled.
KOSCHEI_X402_ENABLED=false
# ARVIS investigation history depth
ARVIS_DEEP_SIG_LIMIT=100
ARVIS_DEEP_TX_PARSE=10
ARVIS_SHALLOW_SIG_LIMIT=20
ARVIS_SHALLOW_TX_PARSE=2
ARVIS_DEEP_OWNER_COUNT=8
ARVIS_SCAN_RPC_BUDGET=600

# Pump live trade capture and on-demand Launch Forensics
PUMPPORTAL_TRADE_SUBSCRIPTION_LIMIT=1000
ARVIS_ATA_SIG_LIMIT=50
ARVIS_ATA_MAX_PAGES=3
ARVIS_FUNDING_CHECK_COUNT=10
ARVIS_FUNDING_RPC_BUDGET=100
ARVIS_FUNDING_SIG_LIMIT=25
ARVIS_FUNDING_TX_PARSE=6
ARVIS_SNIPER_SLOT_WINDOW=3
ARVIS_TRADE_LEDGER_READ_LIMIT=20000
ARVIS_FORENSICS_TIMEOUT_SECONDS=120

# --- Event-driven worker wake-up (release gate #697) -------------------------
# The canonical job, webhook delivery and security alert workers no longer poll
# on a fixed high-frequency tick. Producers signal them in-process; this ceiling
# is the bounded fallback for wake-ups that cannot arrive in-process (another
# instance enqueued the row, or the process restarted mid-flight).
#
# It must exceed the configured Neon idle window, or the compute never suspends
# and gate #698 cannot pass. Clamped to [60, 3600] seconds.
KOSCHEI_WORKER_RECOVERY_CEILING_SECONDS=900

# --- Radar retention archive (release gate #699) ----------------------------
# Expired radar history is archived with a SHA-256 row checksum and deleted in
# the same statement; deletion without a matching archive row cannot occur.
# The archive is a staging ledger: the exporter writes newline-delimited JSON,
# reads the object back, verifies the batch SHA-256, and only then stamps
# exported_at/export_ref. Disabled is the safe default; the backlog ceiling
# remains active and retention fails closed when it is exceeded.
KOSCHEI_RADAR_ARCHIVE_BACKLOG_MAX=2000000
KOSCHEI_RADAR_ARCHIVE_PRUNE_DAYS=7
KOSCHEI_RADAR_ARCHIVE_EXPORT_SINK=disabled
KOSCHEI_RADAR_ARCHIVE_EXPORT_BATCH_SIZE=500
KOSCHEI_RADAR_ARCHIVE_EXPORT_MAX_BATCHES=10
KOSCHEI_RADAR_ARCHIVE_EXPORT_PREFIX=radar-retention
# Filesystem sink (CI/local verification).
KOSCHEI_RADAR_ARCHIVE_EXPORT_FILESYSTEM_PATH=
# S3-compatible production sink. The endpoint may be AWS S3, R2, MinIO,
# or another SigV4-compatible object store. HTTPS is recommended.
KOSCHEI_RADAR_ARCHIVE_EXPORT_S3_ENDPOINT=
KOSCHEI_RADAR_ARCHIVE_EXPORT_S3_BUCKET=
KOSCHEI_RADAR_ARCHIVE_EXPORT_S3_REGION=us-east-1
KOSCHEI_RADAR_ARCHIVE_EXPORT_S3_ACCESS_KEY_ID=
KOSCHEI_RADAR_ARCHIVE_EXPORT_S3_SECRET_ACCESS_KEY=
KOSCHEI_RADAR_ARCHIVE_EXPORT_S3_SESSION_TOKEN=
KOSCHEI_RADAR_ARCHIVE_EXPORT_S3_PATH_STYLE=true


# Polar hosted SaaS billing
# Keep all values server-side. Checkout redirects never authorize product access;
# only verified billing webhooks may mutate the entitlement ledger.
# Koschei exposes a single paid SaaS plan: Professional.
POLAR_ENVIRONMENT=production
POLAR_ACCESS_TOKEN=
POLAR_WEBHOOK_SECRET=
POLAR_PRODUCT_PROFESSIONAL_ID=
# Optional HTTPS-only hosted checkout navigation targets.
POLAR_SUCCESS_URL=https://tradepigloball.co/account?billing=success
POLAR_RETURN_URL=https://tradepigloball.co/pricing

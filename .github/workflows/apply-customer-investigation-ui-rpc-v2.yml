name: Verify customer investigation UI and RPC resilience v2

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  verify:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: koschei/api/go.mod
          cache-dependency-path: koschei/api/go.sum

      - name: Make timeout regression server finite
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('.github/scripts/apply_customer_investigation_ui_rpc.py')
          text = path.read_text()
          old = '<-r.Context().Done()'
          if old not in text:
              raise SystemExit('blocking handler expression not found')
          replacement = 'time.Sleep(200 * time.Millisecond)\n\t\thttp.Error(w, "late primary", http.StatusGatewayTimeout)'
          path.write_text(text.replace(old, replacement, 1))
          PY

      - name: Apply product patch
        run: python .github/scripts/apply_customer_investigation_ui_rpc.py

      - name: Remove obsolete patch workflow
        run: rm -f .github/workflows/apply-customer-investigation-ui-rpc-v2.yml

      - name: Format Go
        working-directory: koschei/api
        run: gofmt -w internal/web3/solana_rpc.go internal/web3/solana_rpc_test.go internal/handlers/unified_live_evidence.go

      - name: Validate customer report UI
        working-directory: koschei/api
        run: |
          node --check public/js/security-radar-detail.js
          node scripts/verify-customer-investigation-ui.js

      - name: Download modules
        working-directory: koschei/api
        run: go mod download

      - name: Verify RPC fallback
        working-directory: koschei/api
        run: go test -v ./internal/web3 -run 'TestSolanaRPC(FallsBackAfterEndpointTimeout|FallsBackAfterProviderRateLimit)' -count=1 -timeout=2m

      - name: Full Go tests
        working-directory: koschei/api
        run: go test ./... -timeout=15m

      - name: Vet
        working-directory: koschei/api
        run: go vet ./...

      - name: Linux build
        working-directory: koschei/api
        run: CGO_ENABLED=0 GOOS=linux go build ./...

      - name: Commit verified product changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: render customer investigation and harden RPC fallback"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}

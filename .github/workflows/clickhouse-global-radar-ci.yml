name: ClickHouse Global Radar CI

on:
  pull_request:
    paths:
      - "koschei/api/clickhouse/migrations/005_global_radar_graph_records.sql"
      - "koschei/api/clickhouse/migrations/005_global_radar_graph_records.sql.sha256"
      - "koschei/api/clickhouse/migrations/006_global_radar_events.sql"
      - "koschei/api/clickhouse/migrations/006_global_radar_events.sql.sha256"
      - "koschei/api/clickhouse/migrations/007_global_radar_ingest_checkpoints.sql"
      - "koschei/api/clickhouse/migrations/007_global_radar_ingest_checkpoints.sql.sha256"
      - "koschei/api/internal/clickhouse/global_radar_event_store.go"
      - "koschei/api/internal/clickhouse/global_radar_event_store_test.go"
      - "koschei/api/internal/clickhouse/global_radar_ingest_checkpoint_store.go"
      - "koschei/api/internal/clickhouse/global_radar_ingest_checkpoint_store_test.go"
      - "koschei/api/internal/radarcursor/**"
      - "koschei/api/internal/radarevent/**"
      - "koschei/api/internal/clickhouse/global_radar_store.go"
      - "koschei/api/internal/clickhouse/global_radar_store_test.go"
      - "koschei/api/internal/services/global_radar_*.go"
      - "koschei/api/internal/services/solana_validator_telemetry.go"
      - "koschei/api/internal/services/solana_validator_telemetry_test.go"
      - "koschei/api/cmd/clickhouse-global-radar-read/**"
      - ".github/workflows/clickhouse-global-radar-ci.yml"
  push:
    branches: [main]
    paths:
      - "koschei/api/clickhouse/migrations/005_global_radar_graph_records.sql"
      - "koschei/api/clickhouse/migrations/005_global_radar_graph_records.sql.sha256"
      - "koschei/api/clickhouse/migrations/006_global_radar_events.sql"
      - "koschei/api/clickhouse/migrations/006_global_radar_events.sql.sha256"
      - "koschei/api/clickhouse/migrations/007_global_radar_ingest_checkpoints.sql"
      - "koschei/api/clickhouse/migrations/007_global_radar_ingest_checkpoints.sql.sha256"
      - "koschei/api/internal/clickhouse/global_radar_event_store.go"
      - "koschei/api/internal/clickhouse/global_radar_event_store_test.go"
      - "koschei/api/internal/clickhouse/global_radar_ingest_checkpoint_store.go"
      - "koschei/api/internal/clickhouse/global_radar_ingest_checkpoint_store_test.go"
      - "koschei/api/internal/radarcursor/**"
      - "koschei/api/internal/radarevent/**"
      - "koschei/api/internal/clickhouse/global_radar_store.go"
      - "koschei/api/internal/clickhouse/global_radar_store_test.go"
      - "koschei/api/internal/services/global_radar_*.go"
      - "koschei/api/cmd/clickhouse-global-radar-read/**"
      - ".github/workflows/clickhouse-global-radar-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validate append-first Global Radar graph memory
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      clickhouse:
        image: clickhouse/clickhouse-server:26.2
        env:
          CLICKHOUSE_DB: koschei_web3
        ports:
          - 8123:8123
        options: >-
          --health-cmd="clickhouse-client --query 'SELECT 1'"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    defaults:
      run:
        working-directory: koschei/api
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: koschei/api/go.mod
          cache-dependency-path: koschei/api/go.sum

      - name: Download modules
        run: go mod download

      - name: Require gofmt-clean Global Radar paths
        run: |
          set -euo pipefail
          unformatted="$(gofmt -l internal/clickhouse/global_radar_store.go internal/clickhouse/global_radar_store_test.go internal/clickhouse/global_radar_event_store.go internal/clickhouse/global_radar_event_store_test.go internal/clickhouse/global_radar_ingest_checkpoint_store.go internal/clickhouse/global_radar_ingest_checkpoint_store_test.go internal/radarcursor/*.go internal/radarevent/*.go internal/networktarget/*_block_ingest.go internal/networktarget/*_block_ingest_test.go internal/services/global_radar_*.go internal/services/solana_validator_telemetry.go internal/services/solana_validator_telemetry_test.go)"
          test -z "${unformatted}" || { printf 'unformatted files:\n%s\n' "${unformatted}" >&2; exit 1; }

      - name: Verify reviewed migration checksum
        run: |
          set -euo pipefail
          cd clickhouse/migrations
          sha256sum -c 005_global_radar_graph_records.sql.sha256
          sha256sum -c 006_global_radar_events.sql.sha256
          sha256sum -c 007_global_radar_ingest_checkpoints.sql.sha256

      - name: Validate migration on ClickHouse 26.2
        run: |
          set -euo pipefail
          container_id="$(docker ps --filter 'ancestor=clickhouse/clickhouse-server:26.2' --format '{{.ID}}' | head -n1)"
          test -n "${container_id}"
          docker exec -i "${container_id}" clickhouse-client --multiquery < clickhouse/migrations/005_global_radar_graph_records.sql
          docker exec -i "${container_id}" clickhouse-client --multiquery < clickhouse/migrations/006_global_radar_events.sql
          docker exec -i "${container_id}" clickhouse-client --multiquery < clickhouse/migrations/007_global_radar_ingest_checkpoints.sql

          table_meta="$(docker exec "${container_id}" clickhouse-client --query "SELECT concat(engine,'|',sorting_key) FROM system.tables WHERE database='koschei_web3' AND name='global_radar_graph_records' FORMAT TSVRaw")"
          payload_hash_type="$(docker exec "${container_id}" clickhouse-client --query "SELECT type FROM system.columns WHERE database='koschei_web3' AND table='global_radar_graph_records' AND name='payload_sha256' FORMAT TSVRaw")"
          observed_type="$(docker exec "${container_id}" clickhouse-client --query "SELECT type FROM system.columns WHERE database='koschei_web3' AND table='global_radar_graph_records' AND name='observed_at' FORMAT TSVRaw")"

          test "${table_meta}" = "ReplacingMergeTree|record_type, network, record_date, subject_id, record_id, payload_sha256"
          test "${payload_hash_type}" = "FixedString(64)"
          test "${observed_type}" = "Nullable(DateTime64(9, 'UTC'))"

          event_table_meta="$(docker exec "${container_id}" clickhouse-client --query "SELECT concat(engine,'|',sorting_key) FROM system.tables WHERE database='koschei_web3' AND name='global_radar_events' FORMAT TSVRaw")"
          event_payload_hash_type="$(docker exec "${container_id}" clickhouse-client --query "SELECT type FROM system.columns WHERE database='koschei_web3' AND table='global_radar_events' AND name='payload_sha256' FORMAT TSVRaw")"
          event_observed_type="$(docker exec "${container_id}" clickhouse-client --query "SELECT type FROM system.columns WHERE database='koschei_web3' AND table='global_radar_events' AND name='observed_at' FORMAT TSVRaw")"

          test "${event_table_meta}" = "ReplacingMergeTree|network_id, event_date, kind, subject_id, event_sha256"
          test "${event_payload_hash_type}" = "FixedString(64)"
          test "${event_observed_type}" = "DateTime64(3, 'UTC')"

          checkpoint_table_meta="$(docker exec "${container_id}" clickhouse-client --query "SELECT concat(engine,\'|\',sorting_key) FROM system.tables WHERE database=\'koschei_web3\' AND name=\'global_radar_ingest_checkpoints\' FORMAT TSVRaw")"
          checkpoint_event_hash_type="$(docker exec "${container_id}" clickhouse-client --query "SELECT type FROM system.columns WHERE database=\'koschei_web3\' AND table=\'global_radar_ingest_checkpoints\' AND name=\'source_event_sha256\' FORMAT TSVRaw")"
          checkpoint_observed_type="$(docker exec "${container_id}" clickhouse-client --query "SELECT type FROM system.columns WHERE database=\'koschei_web3\' AND table=\'global_radar_ingest_checkpoints\' AND name=\'observed_at\' FORMAT TSVRaw")"

          test "${checkpoint_table_meta}" = "ReplacingMergeTree|cursor_key, height, block_hash, source_event_sha256, state"
          test "${checkpoint_event_hash_type}" = "FixedString(64)"
          test "${checkpoint_observed_type}" = "DateTime64(3, \'UTC\')"

      - name: Validate replay convergence and state-history preservation
        run: |
          set -euo pipefail
          container_id="$(docker ps --filter 'ancestor=clickhouse/clickhouse-server:26.2' --format '{{.ID}}' | head -n1)"
          test -n "${container_id}"

          docker exec -i "${container_id}" clickhouse-client --multiquery <<'SQL'
          INSERT INTO koschei_web3.global_radar_graph_records
          (record_type,record_id,schema_version,snapshot_sha256,network,secondary_network,subject_id,target_subject_id,record_kind,evidence_status,evidence_refs,observed_at,recorded_at,payload_json,payload_sha256,ingest_version)
          VALUES
          ('relation','edge-1','koschei.global-radar-relation.v1',repeat('c',64),'ethereum-mainnet','base-mainnet','source-1','target-1','bridge_transfer','unverified',[],NULL,toDateTime64('2026-09-23 19:00:00.000000001',9,'UTC'),'{"state":"unverified"}',repeat('a',64),1),
          ('relation','edge-1','koschei.global-radar-relation.v1',repeat('c',64),'ethereum-mainnet','base-mainnet','source-1','target-1','bridge_transfer','verified',['link-evidence-1'],NULL,toDateTime64('2026-09-23 19:01:00.000000001',9,'UTC'),'{"state":"verified"}',repeat('b',64),2),
          ('relation','edge-1','koschei.global-radar-relation.v1',repeat('c',64),'ethereum-mainnet','base-mainnet','source-1','target-1','bridge_transfer','unverified',[],NULL,toDateTime64('2026-09-23 19:00:00.000000001',9,'UTC'),'{"state":"unverified"}',repeat('a',64),3);
          SQL

          state_versions="$(docker exec "${container_id}" clickhouse-client --query "SELECT count() FROM koschei_web3.global_radar_graph_records FINAL WHERE record_id='edge-1'")"
          unverified_versions="$(docker exec "${container_id}" clickhouse-client --query "SELECT count() FROM koschei_web3.global_radar_graph_records FINAL WHERE record_id='edge-1' AND payload_sha256=repeat('a',64)")"
          verified_versions="$(docker exec "${container_id}" clickhouse-client --query "SELECT count() FROM koschei_web3.global_radar_graph_records FINAL WHERE record_id='edge-1' AND payload_sha256=repeat('b',64)")"
          test "${state_versions}" = "2"
          test "${unverified_versions}" = "1"
          test "${verified_versions}" = "1"

          if docker exec "${container_id}" clickhouse-client --query "INSERT INTO koschei_web3.global_radar_graph_records (record_type,record_id,schema_version,snapshot_sha256,network,subject_id,record_kind,evidence_status,evidence_refs,recorded_at,payload_json,payload_sha256,ingest_version) VALUES ('observation','bad-hash','v1',repeat('c',64),'solana-mainnet','subject','state','observed',[],now64(9),'{}',repeat('z',64),4)"; then
            echo "invalid Global Radar payload hash unexpectedly accepted" >&2
            exit 1
          fi

          docker exec -i "${container_id}" clickhouse-client --multiquery <<'SQL'
          INSERT INTO koschei_web3.global_radar_events
          (event_sha256,schema_version,producer,kind,network_id,subject_kind,subject_id,evidence_state,source_digests,observed_at,payload_json,payload_sha256,ingest_version)
          VALUES
          (repeat('d',64),'koschei.global-radar-event.v1','test-adapter','network_health','ethereum-mainnet','node','node-1','observed',[repeat('e',64)],toDateTime64('2026-09-24 06:00:00.123',3,'UTC'),'{"event":"same"}',repeat('f',64),1),
          (repeat('d',64),'koschei.global-radar-event.v1','test-adapter','network_health','ethereum-mainnet','node','node-1','observed',[repeat('e',64)],toDateTime64('2026-09-24 06:00:00.123',3,'UTC'),'{"event":"same"}',repeat('f',64),2);
          SQL

          event_versions="$(docker exec "${container_id}" clickhouse-client --query "SELECT count() FROM koschei_web3.global_radar_events FINAL WHERE event_sha256=repeat('d',64)")"
          test "${event_versions}" = "1"

          if docker exec "${container_id}" clickhouse-client --query "INSERT INTO koschei_web3.global_radar_events (event_sha256,schema_version,producer,kind,network_id,subject_kind,subject_id,evidence_state,source_digests,observed_at,payload_json,payload_sha256,ingest_version) VALUES (repeat('z',64),'koschei.global-radar-event.v1','test-adapter','network_health','ethereum-mainnet','node','node-2','unavailable',[],now64(3),'{}',repeat('f',64),3)"; then
            echo "invalid Global Radar event digest unexpectedly accepted" >&2
            exit 1
          fi

      - name: Test Global Radar ClickHouse writer and reader
        run: go test ./internal/clickhouse -count=1

      - name: Compile Global Radar memory reader
        run: go test ./cmd/clickhouse-global-radar-read -count=1

      - name: Vet Global Radar ClickHouse paths
        run: go vet ./internal/clickhouse ./cmd/clickhouse-global-radar-read

      - name: Build Global Radar memory reader
        run: CGO_ENABLED=0 GOOS=linux go build ./cmd/clickhouse-global-radar-read

{
  "name": "koschei-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix koschei/frontend",
    "build": "npm run build --prefix koschei/frontend",
    "preview": "npm run preview --prefix koschei/frontend"
  },
  "engines": {
    "node": "20.x"
  }
}

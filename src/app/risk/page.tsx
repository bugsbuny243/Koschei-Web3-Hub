"use client";
import { useMemo, useState } from "react"; import { SiteHeader } from "@/components/SiteHeader"; import { RiskScoreCard } from "@/components/RiskScoreCard";
const items=[["anonymous","Anonymous team?",true],["roadmap","Has roadmap?",false],["repo","Has public repo?",false],["contract","Has token contract?",false],["audit","Has audit?",false],["utility","Has utility?",false],["website","Has website?",false],["community","Has community?",false],["disclaimer","Has clear disclaimers?",false],["custody","Requires custody/private key?",true]] as const;
export default function Risk(){const [checks,setChecks]=useState<Record<string,boolean>>({});const result=useMemo(()=>{let score=20;for(const [key,,negative] of items){if(negative&&checks[key])score+=25;if(!negative&&!checks[key])score+=7}score=Math.min(100,score);const level: "Low"|"Medium"|"High"=score<40?"Low":score<70?"Medium":"High";const fixes=items.filter(([k,,negative])=>negative?checks[k]:!checks[k]).map(([,label,negative])=>negative?`Remove or mitigate: ${label}`:`Add and publish: ${label}`);return{score,level,fixes}},[checks]);return <main className="web3-page"><SiteHeader/><section className="mx-auto max-w-6xl px-5 py-16 lg:px-8"><p className="eyebrow">Trust transparency MVP</p><h1 className="mt-4 text-4xl font-black text-white">Risk & Trust Scanner</h1><div className="mt-9 grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><div className="glass-card grid gap-3 p-6 sm:grid-cols-2">{items.map(([key,label])=><label key={key} className="flex items-center gap-3 rounded-xl border border-white/10 bg-black/10 p-4 text-sm font-bold text-slate-300"><input type="checkbox" checked={!!checks[key]} onChange={e=>setChecks({...checks,[key]:e.target.checked})}/>{label}</label>)}</div><div><RiskScoreCard score={result.score} level={result.level}/><div className="glass-card mt-5 p-6"><h2 className="font-bold text-white">Recommended fixes</h2><ul className="mt-3 space-y-2 text-xs leading-5 text-slate-400">{result.fixes.length?result.fixes.map(x=><li key={x}>• {x}</li>):<li>• Maintain transparent project documentation.</li>}</ul></div></div></div><p className="glass-card sticky bottom-3 mt-6 border-amber-400/20 bg-slate-950/95 p-4 text-xs font-bold leading-5 text-amber-100">This is an informational risk checklist, not legal, financial, investment, or security advice.</p></section></main>}

import { NextResponse } from "next/server"; import { generateWeb3WithTogether, type Web3GenerateMode } from "@/lib/ai/together";
const modes = new Set<Web3GenerateMode>(["metadata","description","pitch","lore","launch"]);
export async function POST(request: Request) { let body: unknown; try { body=await request.json(); } catch { return NextResponse.json({error:"Invalid JSON request body."},{status:400}); } if(typeof body!=="object"||body===null) return NextResponse.json({error:"Invalid request."},{status:400}); const {mode,payload}=body as Record<string,unknown>; if(typeof mode!=="string"||!modes.has(mode as Web3GenerateMode)||typeof payload!=="object"||payload===null||Array.isArray(payload)) return NextResponse.json({error:"Invalid mode or payload."},{status:400}); return NextResponse.json(await generateWeb3WithTogether(mode as Web3GenerateMode,payload as Record<string,unknown>)); }

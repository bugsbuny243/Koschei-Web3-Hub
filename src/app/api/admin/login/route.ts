import { NextResponse } from "next/server";
export async function POST(request:Request){try{const {email,password}=await request.json() as {email?:string;password?:string};const configured=Boolean(process.env.ADMIN_EMAIL&&process.env.ADMIN_PASSWORD);if(!configured)return NextResponse.json({ok:false,error:"Admin credentials are not configured."},{status:503});if(email===process.env.ADMIN_EMAIL&&password===process.env.ADMIN_PASSWORD)return NextResponse.json({ok:true});return NextResponse.json({ok:false,error:"Invalid credentials."},{status:401})}catch{return NextResponse.json({ok:false,error:"Invalid request."},{status:400})}}

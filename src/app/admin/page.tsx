"use client";
import { useEffect,useState } from "react"; import { Container, PageIntro, Panel, Web3Shell } from "@/components/Web3UI";
export default function Admin(){const [ready,setReady]=useState(false),[logged,setLogged]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[count,setCount]=useState(0);useEffect(()=>{const timeout=window.setTimeout(()=>{setLogged(sessionStorage.getItem('koschei-admin-session')==='active');try{setCount(JSON.parse(localStorage.getItem('koschei-assets')||'[]').length)}catch{setCount(0)}setReady(true)},0);return()=>window.clearTimeout(timeout)},[]);async function login(e:React.FormEvent){e.preventDefault();setError('');const data=await (await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})})).json();if(data.ok){sessionStorage.setItem('koschei-admin-session','active');setLogged(true)}else setError(data.error)}return <Web3Shell><Container className="py-16"><PageIntro eyebrow="Admin / Intake" title="Lightweight MVP operations console." text="Simple server-verified credentials and a browser session gate for early operations. This is not a full authentication system."/>{ready&&!logged?<Panel className="mt-10 max-w-md p-6"><form onSubmit={login} className="space-y-4"><label className="block text-xs font-bold">Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-black/30 p-3 text-sm"/></label><label className="block text-xs font-bold">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-black/30 p-3 text-sm"/></label>{error&&<p className="text-sm text-rose-300">{error}</p>}<button className="rounded-lg bg-cyan-400 px-5 py-3 text-xs font-black text-slate-950">Open admin console</button></form></Panel>:ready&&<div className="mt-10 grid gap-4 md:grid-cols-4">{[['Project intake','Placeholder queue'],['Waitlist','Placeholder queue'],['Ecosystem leads','Placeholder queue'],['Generated assets',String(count)]].map(([t,d])=><Panel key={t} className="p-6"><p className="text-sm text-slate-400">{t}</p><p className="mt-4 text-2xl font-black text-cyan-200">{d}</p></Panel>)}</div>}</Container></Web3Shell>}

import { SiteHeader } from "@/components/SiteHeader";
import { AdminDashboard } from "./AdminDashboard";
const keys = ["FEATURE_SOLANA", "FEATURE_BASE", "FEATURE_ARBITRUM", "FEATURE_POLYGON", "FEATURE_OPTIMISM", "FEATURE_ETHEREUM", "FEATURE_RISK_SCANNER", "FEATURE_METADATA_STUDIO", "FEATURE_GAME_ASSET_BRIDGE", "FEATURE_LAUNCH_PAGE_BUILDER", "FEATURE_GRANT_OS", "FEATURE_TOKEN_TRADING", "FEATURE_CUSTODY", "FEATURE_PRIVATE_KEY_DEPLOY"];
export default function Admin() { const flags = Object.fromEntries(keys.map((key) => [key, process.env[key]?.toLowerCase() === "true"])); return <main className="web3-page"><SiteHeader /><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><p className="eyebrow">Neon-backed administration</p><h1 className="mt-4 text-4xl font-black text-white">Admin / Intake</h1><p className="mt-4 text-sm text-slate-400">Review products, manually submitted orders, verified entitlements and generated outputs. Pending orders never activate rights until payment is verified by an administrator.</p><AdminDashboard flags={flags} /></section></main>; }

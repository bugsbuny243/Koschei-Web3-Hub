import { SiteHeader } from "@/components/SiteHeader"; import { ChainHealthCard } from "@/components/ChainHealthCard";
const chains=[["solana","Solana Devnet"],["base","Base Sepolia"],["arbitrum","Arbitrum Sepolia"],["polygon","Polygon Amoy"],["optimism","Optimism Sepolia"],["ethereum","Ethereum Sepolia"]];
export default function Chains(){return <main className="web3-page"><SiteHeader/><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><p className="eyebrow">Provider diagnostics</p><h1 className="mt-4 text-4xl font-black text-white">ChainOps Dashboard</h1><p className="mt-4 max-w-3xl text-sm leading-7 text-slate-400">Run server-side health checks for supported test environments. Provider keys and full RPC URLs never enter the browser response.</p><div className="mt-9 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{chains.map(([chain,network])=><ChainHealthCard key={chain} chain={chain} network={network}/>)}</div></section></main>}

[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckTimeout = 3600
startCommand = "/app/start.sh"

[[volumes]]
name = "android-toolchains"
mountPath = "/opt/android-cache"

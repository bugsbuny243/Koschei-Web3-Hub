(()=>{
'use strict';
const $=id=>document.getElementById(id);
async function api(path,opt={}){const h=new Headers(opt.headers||{});if(opt.body)h.set('Content-Type','application/json');const r=await fetch(path,{credentials:'same-origin',...opt,headers:h});let d={};try{d=await r.json()}catch{}if(!r.ok||d.ok===false)throw new Error(d.message||d.detail||d.error||`İstek başarısız (${r.status})`);return d}
function close(){const root=$('modalRoot');if(root)root.innerHTML=''}
function download(url,title){const a=document.createElement('a');a.href=url;a.download=`${title||'koschei-mobile-game'}-expo.zip`;document.body.appendChild(a);a.click();a.remove()}
function open(){const root=$('modalRoot');root.innerHTML=`<div class="modal-backdrop" id="gameBackdrop"><section class="modal"><div class="modal-head"><div><span class="eyebrow">Owner-only tool</span><h2>Mobil Oyun Studio</h2><p class="small muted">Oyun fikrini AI tasarımına ve çalıştırılabilir Expo kaynak paketine çevirir.</p></div><button class="modal-close" id="gameClose">×</button></div><form id="gameForm"><div class="field"><label>Oyun adı</label><input class="input" id="gameTitle" maxlength="120" placeholder="Neon Solana Runner" required></div><div class="field"><label>Hedef</label><select class="select" id="gamePlatform"><option value="android_game">Android</option><option value="web_and_android">Web + Android</option></select></div><div class="field"><label>Oyun fikri</label><textarea class="textarea" id="gamePrompt" maxlength="5000" placeholder="Tema, karakter, düşmanlar ve kazanma koşulunu anlat." required></textarea></div><button class="btn primary" id="gameGenerate" style="width:100%;margin-top:14px" type="submit">Otomatik oyunu oluştur ve indir</button><div class="small muted" id="gameStatus" style="margin-top:10px">Hazır.</div></form></section></div>`;$('gameClose').onclick=close;$('gameBackdrop').onclick=e=>{if(e.target.id==='gameBackdrop')close()};$('gameForm').onsubmit=create}
async function create(e){e.preventDefault();const b=$('gameGenerate'),s=$('gameStatus');b.disabled=true;b.textContent='AI oyunu hazırlıyor…';s.textContent='Spesifikasyon ve Expo paketi üretiliyor.';try{const d=await api('/api/owner/game-studio',{method:'POST',body:JSON.stringify({title:$('gameTitle').value.trim(),prompt:$('gamePrompt').value.trim(),target_platform:$('gamePlatform').value})});s.textContent=d.message||'Oyun hazır.';if(d.project?.bundle_url)download(d.project.bundle_url,d.project.title)}catch(err){s.textContent=err.message}finally{b.disabled=false;b.textContent='Otomatik oyunu oluştur ve indir'}}
const button=$('gameStudioButton');if(button)button.onclick=open;
})();

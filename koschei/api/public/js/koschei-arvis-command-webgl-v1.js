// ORPHAN: no HTML page loads this file as of 2026-09-14.
(()=>{'use strict';const host=document.getElementById('arvis-webgl');if(!host)return;const c=document.createElement('canvas');host.appendChild(c);const gl=c.getContext('webgl',{alpha:true,antialias:true,premultipliedAlpha:false});if(!gl)return;const vs=`attribute vec3 p;attribute vec3 col;attribute float sz;uniform vec2 res;uniform float time;varying vec3 vcol;void main(){float z=1.0+p.z*.32;vec2 q=p.xy*z;q.x*=res.y/res.x;float breathe=1.0+.012*sin(time*.7+p.z*7.);gl_Position=vec4(q*breathe,0.,1.);gl_PointSize=sz*z;vcol=col;}`;const fs=`precision mediump float;varying vec3 vcol;void main(){vec2 q=gl_PointCoord-.5;float d=length(q);float a=smoothstep(.5,.08,d);float core=smoothstep(.17,0.,d);gl_FragColor=vec4(vcol,a*.52+core*.48);}`;function sh(t,s){const x=gl.createShader(t);gl.shaderSource(x,s);gl.compileShader(x);return x}const pr=gl.createProgram();gl.attachShader(pr,sh(gl.VERTEX_SHADER,vs));gl.attachShader(pr,sh(gl.FRAGMENT_SHADER,fs));gl.linkProgram(pr);gl.useProgram(pr);const P=gl.getAttribLocation(pr,'p'),C=gl.getAttribLocation(pr,'col'),S=gl.getAttribLocation(pr,'sz'),R=gl.getUniformLocation(pr,'res'),T=gl.getUniformLocation(pr,'time');let seed=9147;function rnd(){seed=(seed*1664525+1013904223)>>>0;return seed/4294967296}const nodes=[],groups=8,N=420;for(let g=0;g<groups;g++){const ga=g/groups*Math.PI*2-Math.PI/2,gr=.47;const gx=Math.cos(ga)*gr,gy=Math.sin(ga)*gr;const count=g===0?64:Math.floor((N-64)/(groups-1));for(let i=0;i<count;i++){const a=rnd()*Math.PI*2,r=Math.pow(rnd(),.62)*.18;let col=[.12,.65,1];if(g===3)col=[1,.42,.10];if(g===4)col=[1,.12,.18];if(g===6)col=[.45,.25,1];nodes.push({x:gx+Math.cos(a)*r,y:gy+Math.sin(a)*r,z:rnd()*2-1,col,s:2.2+rnd()*4.2,g});}}nodes.push({x:0,y:0,z:.8,col:[.65,.92,1],s:14,g:99});const edges=[];for(let i=0;i<nodes.length-1;i++){let best=-1,bd=99;for(let j=0;j<nodes.length-1;j++){if(i===j)continue;const a=nodes[i],b=nodes[j],d=(a.x-b.x)**2+(a.y-b.y)**2;if(d<bd&&((a.g===b.g)||rnd()<.018)){bd=d;best=j}}if(best>=0)edges.push([i,best]);if(rnd()<.16)edges.push([i,nodes.length-1]);}function buf(data,loc,size){const b=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(data),gl.STATIC_DRAW);gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,size,gl.FLOAT,false,0,0);return b}const pos=[],colors=[],sizes=[];nodes.forEach(n=>{pos.push(n.x,n.y,n.z);colors.push(...n.col);sizes.push(n.s)});const ep=[],ec=[];edges.forEach(([i,j])=>{const a=nodes[i],b=nodes[j];ep.push(a.x,a.y,a.z,b.x,b.y,b.z);const col=(a.g===4||b.g===4)?[1,.12,.18]:(a.g===3||b.g===3)?[1,.42,.10]:[.08,.52,.86];ec.push(...col,...col)});const pb=buf(pos,P,3),cb=buf(colors,C,3),sb=buf(sizes,S,1),eb=gl.createBuffer(),eCol=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,eb);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(ep),gl.STATIC_DRAW);gl.bindBuffer(gl.ARRAY_BUFFER,eCol);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(ec),gl.STATIC_DRAW);function resize(){const d=Math.min(devicePixelRatio||1,1.7),r=host.getBoundingClientRect();c.width=r.width*d;c.height=r.height*d;gl.viewport(0,0,c.width,c.height)}addEventListener('resize',resize,{passive:true});resize();gl.enable(gl.BLEND);gl.blendFunc(gl.SRC_ALPHA,gl.ONE);function attrib(b,l,s){gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.enableVertexAttribArray(l);gl.vertexAttribPointer(l,s,gl.FLOAT,false,0,0)}function draw(t){gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT);gl.uniform2f(R,c.width,c.height);gl.uniform1f(T,t*.001);attrib(eb,P,3);attrib(eCol,C,3);gl.disableVertexAttribArray(S);gl.vertexAttrib1f(S,1);gl.drawArrays(gl.LINES,0,ep.length/3);attrib(pb,P,3);attrib(cb,C,3);attrib(sb,S,1);gl.drawArrays(gl.POINTS,0,nodes.length);requestAnimationFrame(draw)}requestAnimationFrame(draw)})();
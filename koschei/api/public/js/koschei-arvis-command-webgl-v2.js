// ORPHAN: no HTML page loads this file as of 2026-09-14.
(()=>{'use strict';const host=document.getElementById('arvis-webgl');if(!host)return;const c=document.createElement('canvas');host.appendChild(c);const gl=c.getContext('webgl',{alpha:true,antialias:true,premultipliedAlpha:false});if(!gl)return;const VS=`attribute vec3 p;attribute vec3 col;attribute float sz;uniform vec2 res;uniform float time;varying vec3 vcol;void main(){float ay=time*.055;float ax=.23+sin(time*.11)*.035;mat3 ry=mat3(cos(ay),0.,sin(ay),0.,1.,0.,-sin(ay),0.,cos(ay));mat3 rx=mat3(1.,0.,0.,0.,cos(ax),-sin(ax),0.,sin(ax),cos(ax));vec3 q=rx*ry*p;float cam=2.35;float inv=1.0/(cam-q.z);vec2 pr=q.xy*inv*2.45;pr.x*=res.y/res.x;gl_Position=vec4(pr,0.,1.);gl_PointSize=sz*(.9+inv*2.3);vcol=col;}`;const FS=`precision mediump float;varying vec3 vcol;void main(){vec2 q=gl_PointCoord-.5;float d=length(q);float a=smoothstep(.5,.05,d);float core=smoothstep(.15,0.,d);gl_FragColor=vec4(vcol,a*.45+core*.72);}`;function shader(t,s){const x=gl.createShader(t);gl.shaderSource(x,s);gl.compileShader(x);return x}const pr=gl.createProgram();gl.attachShader(pr,shader(gl.VERTEX_SHADER,VS));gl.attachShader(pr,shader(gl.FRAGMENT_SHADER,FS));gl.linkProgram(pr);gl.useProgram(pr);const P=gl.getAttribLocation(pr,'p'),C=gl.getAttribLocation(pr,'col'),S=gl.getAttribLocation(pr,'sz'),R=gl.getUniformLocation(pr,'res'),T=gl.getUniformLocation(pr,'time');let seed=314159;function rnd(){seed=(seed*1664525+1013904223)>>>0;return seed/4294967296}const nodes=[];const groups=[[-.54,.34,.17,0],[-.10,.58,.15,0],[.43,.42,.17,1],[.61,.04,.18,2],[.44,-.43,.18,3],[.02,-.61,.16,4],[-.44,-.43,.17,5],[-.61,.02,.18,0]];groups.forEach((g,gi)=>{const count=gi===0?240:170;for(let i=0;i<count;i++){const a=rnd()*Math.PI*2,b=Math.acos(2*rnd()-1),rr=.05+Math.pow(rnd(),.62)*g[2];let col=[.08,.68,1];if(g[3]===1)col=[1,.68,.12];if(g[3]===2)col=[1,.36,.07];if(g[3]===3)col=[1,.10,.16];if(g[3]===4)col=[.52,.34,1];if(g[3]===5)col=[.16,.88,.97];nodes.push({x:g[0]+Math.sin(b)*Math.cos(a)*rr,y:g[1]+Math.sin(b)*Math.sin(a)*rr,z:Math.cos(b)*rr*1.9,col,s:1.5+rnd()*4.1,g:gi});}});for(let i=0;i<430;i++){const a=rnd()*Math.PI*2,u=rnd()*2-1,r=.49+rnd()*.27,s=Math.sqrt(1-u*u);nodes.push({x:s*Math.cos(a)*r,y:u*r,z:s*Math.sin(a)*r,col:[.08,.60,.96],s:1.1+rnd()*2.6,g:20});}nodes.push({x:0,y:0,z:.08,col:[.72,.95,1],s:14,g:99});const center=nodes.length-1,edges=[];for(let i=0;i<nodes.length-1;i++){const a=nodes[i],best=[];for(let j=0;j<nodes.length-1;j++){if(i===j)continue;const b=nodes[j],d=(a.x-b.x)**2+(a.y-b.y)**2+(a.z-b.z)**2;if((a.g===b.g&&d<.032)||(a.g===20&&b.g===20&&d<.022))best.push([d,j]);}best.sort((x,y)=>x[0]-y[0]);for(let k=0;k<Math.min(3,best.length);k++)if(best[k][1]>i)edges.push([i,best[k][1]]);if(i%11===0)edges.push([i,center]);if(i%37===0){const j=Math.floor(rnd()*(nodes.length-1));edges.push([i,j]);}}const wire=[];function addWire(a,b,col){wire.push(a,b,col)}for(let lat=-60;lat<=60;lat+=20){let prev=null;const ph=lat*Math.PI/180;for(let lon=0;lon<=360;lon+=7){const th=lon*Math.PI/180,r=.79,p=[Math.cos(ph)*Math.cos(th)*r,Math.sin(ph)*r,Math.cos(ph)*Math.sin(th)*r];if(prev)addWire(prev,p,[.05,.33,.55]);prev=p}}for(let lon=0;lon<360;lon+=24){let prev=null;const th=lon*Math.PI/180;for(let lat=-90;lat<=90;lat+=6){const ph=lat*Math.PI/180,r=.79,p=[Math.cos(ph)*Math.cos(th)*r,Math.sin(ph)*r,Math.cos(ph)*Math.sin(th)*r];if(prev)addWire(prev,p,[.05,.33,.55]);prev=p}}function make(data,loc,size){const b=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(data),gl.STATIC_DRAW);gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,size,gl.FLOAT,false,0,0);return b}const pos=[],cols=[],sizes=[];nodes.forEach(n=>{pos.push(n.x,n.y,n.z);cols.push(...n.col);sizes.push(n.s)});const ep=[],ec=[];edges.forEach(([i,j])=>{const a=nodes[i],b=nodes[j];ep.push(a.x,a.y,a.z,b.x,b.y,b.z);const hot=(a.g===4||b.g===4),sus=(a.g===3||b.g===3),watch=(a.g===2||b.g===2),col=hot?[1,.10,.16]:sus?[1,.36,.07]:watch?[1,.68,.12]:[.06,.50,.86];ec.push(...col,...col)});wire.forEach(([a,b,col])=>{ep.push(...a,...b);ec.push(...col,...col)});const pb=make(pos,P,3),cb=make(cols,C,3),sb=make(sizes,S,1);const eb=gl.createBuffer(),eCol=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,eb);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(ep),gl.STATIC_DRAW);gl.bindBuffer(gl.ARRAY_BUFFER,eCol);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(ec),gl.STATIC_DRAW);function attrib(b,l,s){gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.enableVertexAttribArray(l);gl.vertexAttribPointer(l,s,gl.FLOAT,false,0,0)}function resize(){const d=Math.min(devicePixelRatio||1,1.6),r=host.getBoundingClientRect();c.width=Math.max(1,r.width*d);c.height=Math.max(1,r.height*d);gl.viewport(0,0,c.width,c.height)}addEventListener('resize',resize,{passive:true});resize();gl.enable(gl.BLEND);gl.blendFunc(gl.SRC_ALPHA,gl.ONE);function draw(t){gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT);gl.uniform2f(R,c.width,c.height);gl.uniform1f(T,t*.001);attrib(eb,P,3);attrib(eCol,C,3);gl.disableVertexAttribArray(S);gl.vertexAttrib1f(S,1);gl.drawArrays(gl.LINES,0,ep.length/3);attrib(pb,P,3);attrib(cb,C,3);attrib(sb,S,1);gl.drawArrays(gl.POINTS,0,nodes.length);requestAnimationFrame(draw)}requestAnimationFrame(draw)})();
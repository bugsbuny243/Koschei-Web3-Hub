(()=>{
'use strict';

const NAV=[
  {id:'command',icon:'⌂',label:'Özet',title:'Owner Operation Center',eyebrow:'Operasyon merkezi'},
  {id:'arvis',icon:'◉',label:'Radar',title:'ARVIS Radar',eyebrow:'Canlı risk altyapısı'},
  {id:'customers',icon:'◎',label:'Müşteriler',title:'Müşteriler',eyebrow:'Üyelik ve erişim'},
  {id:'revenue',icon:'₺',label:'Gelir',title:'Gelir & Paketler',eyebrow:'Ödeme ve entitlement'},
  {id:'security',icon:'◇',label:'Güvenlik',title:'Güvenlik Olayları',eyebrow:'Audit görünümü'},
  {id:'system',icon:'⚙',label:'Sistem',title:'Sistem Sağlığı',eyebrow:'Servis durumu'},
  {id:'brain',icon:'◆',label:'Brain',title:'Owner Brain',eyebrow:'Operasyon copilot'}
];

const state={active:'command',dashboard:null,users:[],payments:[],paymentHealth:null,feedback:[],selectedFeedback:null,security:[],radarSources:[],loading:false};
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=v=>new Intl.NumberFormat('tr-TR').format(Number(v||0));
const money=v=>new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY',maximumFractionDigits:2}).format(Number(v||0)/100);
const dt=v=>{if(!v)return'—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('tr-TR',{dateStyle:'short',timeStyle:'short'}).format(d)};
const short=(v,n=24)=>{v=String(v||'');return v.length>n?v.slice(0,Math.max(8,n-10))+'…'+v.slice(-7):v||'—'};
const sum=(items,key)=>items.reduce((a,x)=>a+Number(x?.[key]||0),0);

async function api(path,opt={}){
  const headers=new Headers(opt.headers||{});if(opt.body&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');
  const response=await fetch(path,{credentials:'same-origin',...opt,headers});
  let data={};try{data=await response.json()}catch{}
  if(!response.ok||data.ok===false||data.success===false){const error=new Error(data.message||data.error||`İstek başarısız (${response.status})`);error.status=response.status;error.data=data;throw error}
  return data;
}
function toast(message,bad=false){const el=$('toast');el.textContent=message;el.style.borderColor=bad?'#ff526f55':'#1de6c855';el.className='toast show';clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.className='toast',3800)}
function statusValue(raw){if(typeof raw==='string')return raw;if(raw&&typeof raw==='object'){return raw.status||raw.state||(raw.configured===true?'configured':'unknown')}return'unknown'}
function tone(value){const s=String(value||'').toLowerCase();if(['healthy','connected','configured','active','success','enabled','ok','live','ready'].some(x=>s.includes(x)))return'ok';if(['failed','error','critical','fatal','unavailable','banned','degraded','stale','missing'].some(x=>s.includes(x)))return'bad';return'warn'}
function badge(value){return`<span class="badge ${tone(value)}">${esc(value||'unknown')}</span>`}
function setSync(text,kind='warn'){const dot=$('syncDot');dot.className='dot'+(kind==='bad'?' bad':kind==='ok'?'':' warn');$('syncText').textContent=text}
function showLogin(message=''){$('loginView').classList.remove('hidden');$('appView').classList.add('hidden');if(message){$('loginError').textContent=message;$('loginError').classList.remove('hidden')}}
function showApp(){$('loginView').classList.add('hidden');$('appView').classList.remove('hidden')}

function renderNavigation(){
  const html=NAV.map(n=>`<button class="nav-item ${state.active===n.id?'active':''}" data-nav="${n.id}"><span class="nav-icon">${n.icon}</span><span>${n.label}</span><span class="nav-badge ${navCount(n.id)?'':'hidden'}">${navCount(n.id)}</span></button>`).join('');
  $('desktopNav').innerHTML=html;
  $('mobileNav').innerHTML=NAV.map(n=>`<button class="${state.active===n.id?'active':''}" data-nav="${n.id}"><span>${n.icon}</span>${n.label}</button>`).join('');
  document.querySelectorAll('[data-nav]').forEach(button=>button.onclick=()=>showPage(button.dataset.nav));
}
function navCount(id){const s=state.dashboard?.summary||{};if(id==='revenue')return Number(s.pending_payments||0);if(id==='security')return Number(s.critical_security_events_24h||0);if(id==='arvis')return Number(state.dashboard?.arvis?.processing_failed_recent||0);if(id==='customers')return Number(s.total_users||0);return 0}
async function showPage(id){if(!NAV.some(n=>n.id===id))return;state.active=id;const item=NAV.find(n=>n.id===id);$('pageTitle').textContent=item.title;$('pageEyebrow').textContent=item.eyebrow;document.querySelectorAll('.page').forEach(p=>p.classList.toggle('active',p.id===`page-${id}`));renderNavigation();await loadPage(id)}

function kpi(label,value,foot,toneClass='',icon=''){return`<article class="card kpi"><div class="kpi-top"><div><div class="kpi-label">${esc(label)}</div><div class="kpi-value ${toneClass}">${esc(value)}</div></div>${icon?`<span class="kpi-icon">${icon}</span>`:''}</div><div class="kpi-foot">${esc(foot)}</div></article>`}
function compactServiceGrid(services={}){const labels={database:'Database',neon_auth:'Neon Auth',solana_rpc:'Solana RPC',paddle:'Paddle',shopier:'Shopier',security_radar:'ARVIS Radar'};const order=['database','neon_auth','paddle','security_radar','solana_rpc','shopier'];return`<div class="service-strip">${order.filter(k=>services[k]!==undefined).map(key=>{const s=statusValue(services[key]);return`<button class="service-mini" data-nav="${key==='security_radar'?'arvis':key==='paddle'||key==='shopier'?'revenue':'system'}"><b>${esc(labels[key]||key)}</b>${badge(s)}<small>${serviceNote(key,s)}</small></button>`}).join('')}</div>`}
function serviceNote(key,status){if(key==='shopier')return'Manuel ödeme';if(key==='security_radar')return'Pipeline & worker';return tone(status)==='ok'?'Operasyonel':'Kontrol gerekli'}
function chart(items=[],key='count',currency=false){if(!items.length)return'<div class="empty compact">Trend verisi yok.</div>';const max=Math.max(1,...items.map(x=>Number(x[key]||0)));return`<div class="chart slim">${items.map(x=>{const value=Number(x[key]||0),pct=Math.max(4,Math.round(value/max*100));return`<div class="bar-col" title="${currency?money(value):num(value)}"><div class="bar" style="--value:${pct}"></div><small>${esc(String(x.date||'').slice(5))}</small></div>`}).join('')}</div>`}
function summaryRow(label,value,status='ok'){return`<div class="summary-row"><span>${esc(label)}</span><b>${esc(value)}</b>${badge(status)}</div>`}

function renderCommand(){const d=state.dashboard||{},s=d.summary||{},t=d.trends||{},a=d.arvis||{},services=d.services||{};$('commandContent').innerHTML=`
<div class="owner-home">
  <section class="hero-card card">
    <div><span class="eyebrow">Tek ekran operasyon</span><h2>Kritik durum</h2><p class="muted">Radar, ödeme, müşteri ve güvenlik özetleri tek yerde. Detay için ilgili karta dokun.</p></div>
    <div class="hero-status"><b>${String(a.pipeline_status||'unknown').replaceAll('_',' ')}</b><span>ARVIS pipeline</span></div>
  </section>
  <section class="grid compact-grid">
    ${kpi('Kullanıcı',num(s.total_users),`Son 7 gün +${num(s.new_users_7d)}`,'tone-cyan','◎')}
    ${kpi('Aktif paket',num(s.active_entitlements),`${num(s.expiring_entitlements_7d)} paket 7 günde bitiyor`,'tone-green','◈')}
    ${kpi('ARVIS 24s',num(s.radar_findings_24h),`${num(s.risk_cards_24h)} risk · ${num(s.monitor_cards_24h)} monitor`,'tone-cyan','◉')}
    ${kpi('30g gelir',money(s.revenue_try_cents_30d),`${num(s.paid_orders_30d)} başarılı sipariş`,'tone-green','₺')}
    ${kpi('Bekleyen ödeme',num(s.pending_payments),'Owner onayı gerekiyor',s.pending_payments?'tone-amber':'tone-green','⌛')}
    ${kpi('Güvenlik 24s',num(s.security_events_24h),`${num(s.critical_security_events_24h)} kritik`,s.critical_security_events_24h?'tone-red':'tone-green','◇')}
  </section>
  <section class="grid compact-grid">
    <article class="card span-8"><div class="card-head"><div><span class="eyebrow">Canlı servisler</span><h2>Operasyon durumu</h2></div></div>${compactServiceGrid(services)}</article>
    <article class="card span-4"><div class="card-head"><div><span class="eyebrow">Owner aksiyon</span><h2>Bugün ne var?</h2></div></div><div class="clean-list">${(d.action_queue||[]).slice(0,4).map(a=>`<button class="clean-action ${esc(a.priority)}" data-action-tab="${esc(a.target_tab)}"><b>${esc(a.title)}</b><span>${esc(a.detail)}</span><i>${num(a.count)}</i></button>`).join('')||'<div class="success-box">Kritik owner aksiyonu yok.</div>'}</div></article>
    <article class="card span-7"><div class="card-head"><div><span class="eyebrow">ARVIS proof</span><h2>Radar üretim kanıtı</h2></div><button class="btn small" data-nav="arvis">Radar aç</button></div><div class="proof-grid">${summaryRow('Pipeline',String(a.pipeline_status||'unknown').replaceAll('_',' '),a.pipeline_status)}${summaryRow('Processed',num(a.processing_completed),'completed')}${summaryRow('Failed',num(a.processing_failed_recent),'ok')}${summaryRow('Runtime arms',num(a.runtime_engines),'live')}</div></article>
    <article class="card span-5"><div class="card-head"><div><span class="eyebrow">7 günlük trend</span><h2>Kullanıcı & gelir</h2></div></div><div class="mini-trends"><div><b>${num(s.new_users_7d)}</b><span>Yeni kullanıcı</span>${chart(t.users_7d,'count')}</div><div><b>${money(sum(t.orders_7d||[],'revenue_try_cents'))}</b><span>Gelir</span>${chart(t.orders_7d,'revenue_try_cents',true)}</div></div></article>
  </section>
</div>`;
  document.querySelectorAll('[data-action-tab]').forEach(b=>b.onclick=()=>showPage(b.dataset.actionTab));
  document.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>showPage(b.dataset.nav));
}

async function loadArvis(){const content=$('arvisContent');content.innerHTML='<div class="card loading">ARVIS operasyonları yükleniyor…</div>';try{const [sources]=await Promise.all([api('/api/owner/radar/sources')]);state.radarSources=sources.sources||[];renderArvis()}catch(e){content.innerHTML=`<div class="error-box">${esc(e.message)}</div>`}}
function renderArvis(){const a=state.dashboard?.arvis||{},sources=a.sources||{},fail=a.failures||{};const sourceBlock=(name,data={})=>`<div class="source-card clean-source"><div class="card-head"><h3>${esc(name)}</h3>${badge(Number(data.recent||0)>0?'live':Number(data.events||0)>0?'stale':'waiting')}</div><div class="source-stats"><div><label>Toplam</label><b>${num(data.events)}</b></div><div><label>15 dk</label><b>${num(data.recent)}</b></div><div><label>Mint</label><b>${num(data.enriched)}</b></div></div><div class="small muted">Son olay: ${dt(data.last_event_at)}</div></div>`;
  $('arvisContent').innerHTML=`<div class="grid compact-grid">
    ${kpi('Pipeline',String(a.pipeline_status||'unknown').replaceAll('_',' '),'Canlı veri + evidence worker',tone(a.pipeline_status)==='bad'?'tone-red':tone(a.pipeline_status)==='ok'?'tone-green':'tone-amber','◉')}
    ${kpi('Processed',num(a.processing_completed),`${num(a.processing_active)} aktif iş`,'tone-green','✓')}
    ${kpi('Runtime arms',num(a.runtime_engines),'Son 15 dakikada kanıt üreten kollar','tone-cyan','◇')}
    ${kpi('Failed',num(a.processing_failed),`${num(a.processing_failed_recent)} yakın zamanlı`,a.processing_failed_recent?'tone-red':'tone-green','!')}
    <article class="card span-8"><div class="card-head"><div><span class="eyebrow">Canlı kaynaklar</span><h2>Program gözlemcileri</h2></div></div><div class="source-cards">${sourceBlock('Pump.fun / PumpSwap',sources.pump)}${sourceBlock('Raydium',sources.raydium)}</div></article>
    <article class="card span-4"><div class="card-head"><div><span class="eyebrow">Queue health</span><h2>İşlem kuyruğu</h2></div>${badge(a.pipeline_status)}</div><div class="proof-grid vertical">${summaryRow('Retryable',num(fail.retryable),fail.retryable?'warn':'ok')}${summaryRow('Exhausted',num(fail.exhausted),fail.exhausted?'bad':'ok')}${summaryRow('Son 15 dk hata',num(fail.recent_15_minutes),fail.recent_15_minutes?'warn':'ok')}${summaryRow('Son hata',fail.latest_error_code||'none',fail.latest_error_code?'warn':'ok')}</div></article>
    <article class="card span-12"><details class="owner-details" open><summary><span><b>Radar kaynak kayıt defteri</b><small>Owner doğrulamalı kaynaklar</small></span></summary><form id="sourceForm" class="filters clean-form"><select class="select" id="sourceModule"><option value="pump_sybil_radar">Pump.fun Sybil Radar</option><option value="raydium_pool_guardian">Raydium Pool Guardian</option><option value="walletless_claim_shield">Walletless Claim Shield</option></select><input class="input" id="sourceLabel" placeholder="Kaynak etiketi"><input class="input mono" id="sourceAddress" placeholder="Solana program / source adresi"><input class="input" id="sourceNetwork" value="solana-mainnet"><button class="btn primary" type="submit">Kaydet</button></form><div id="sourceRegistry" style="margin-top:12px">${renderSourceRegistry()}</div></details></article>
  </div>`;
  $('sourceForm').onsubmit=createRadarSource;document.querySelectorAll('[data-source-toggle]').forEach(b=>b.onclick=()=>toggleRadarSource(b.dataset.sourceToggle,b.dataset.enabled!=='true'));document.querySelectorAll('[data-source-delete]').forEach(b=>b.onclick=()=>deleteRadarSource(b.dataset.sourceDelete));
}
function renderSourceRegistry(){if(!state.radarSources.length)return'<div class="empty compact">Owner kayıtlı ek kaynak yok.</div>';return`<div class="table-wrap"><table class="table compact-table"><thead><tr><th>Modül</th><th>Etiket</th><th>Adres</th><th>Ağ</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>${state.radarSources.map(s=>`<tr><td>${esc(s.module_id)}</td><td>${esc(s.label||s.name||'—')}</td><td class="mono">${esc(short(s.address||s.target,48))}</td><td>${esc(s.network||'solana-mainnet')}</td><td>${badge(s.enabled?'enabled':'disabled')}</td><td><button class="btn small" data-source-toggle="${esc(s.id)}" data-enabled="${s.enabled?'true':'false'}">${s.enabled?'Durdur':'Aç'}</button> <button class="btn small danger" data-source-delete="${esc(s.id)}">Sil</button></td></tr>`).join('')}</tbody></table></div>`}
async function createRadarSource(e){e.preventDefault();const body={module_id:$('sourceModule').value,label:$('sourceLabel').value.trim(),address:$('sourceAddress').value.trim(),target:$('sourceAddress').value.trim(),network:$('sourceNetwork').value.trim()||'solana-mainnet',enabled:true};if(!body.address)return toast('Kaynak adresi gerekli.',true);try{await api('/api/owner/radar/sources',{method:'POST',body:JSON.stringify(body)});toast('Kaynak kaydedildi.');await loadArvis()}catch(err){toast(err.message,true)}}
async function toggleRadarSource(id,enabled){try{const source=state.radarSources.find(x=>String(x.id)===String(id));if(!source)return;await api('/api/owner/radar/sources',{method:'PATCH',body:JSON.stringify({...source,id,enabled})});toast(enabled?'Kaynak açıldı.':'Kaynak durduruldu.');await loadArvis()}catch(e){toast(e.message,true)}}
async function deleteRadarSource(id){if(!confirm('Bu doğrulanmış kaynağı silmek istediğine emin misin?'))return;try{await api('/api/owner/radar/sources',{method:'DELETE',body:JSON.stringify({id})});toast('Kaynak silindi.');await loadArvis()}catch(e){toast(e.message,true)}}

async function loadCustomers(query=''){const content=$('customersContent');content.innerHTML='<div class="card loading">Müşteriler yükleniyor…</div>';try{const d=await api('/api/owner/users'+(query?`?q=${encodeURIComponent(query)}`:''));state.users=d.users||[];renderCustomers()}catch(e){content.innerHTML=`<div class="error-box">${esc(e.message)}</div>`}}
function renderCustomers(){const plans=[...new Set(state.users.map(u=>u.plan_id).filter(Boolean))];$('customersContent').innerHTML=`<article class="card"><div class="toolbar"><div><span class="eyebrow">Customer operations</span><h2>Müşteri ve erişim yönetimi</h2></div><div class="filters"><input class="input" id="customerSearch" placeholder="E-posta, auth ID veya wallet"><select class="select" id="customerStatus"><option value="">Tüm durumlar</option><option value="active">Aktif</option><option value="banned">Yasaklı</option><option value="removed">Kaldırılmış</option></select><select class="select" id="customerPlan"><option value="">Tüm paketler</option>${plans.map(p=>`<option>${esc(p)}</option>`).join('')}</select><button class="btn" id="customerSearchButton">Ara</button></div></div><div id="customerTable">${customerTable(state.users)}</div></article>`;$('customerSearchButton').onclick=()=>loadCustomers($('customerSearch').value.trim());$('customerSearch').onkeydown=e=>{if(e.key==='Enter')loadCustomers(e.target.value.trim())};$('customerStatus').onchange=filterCustomers;$('customerPlan').onchange=filterCustomers;bindUserButtons()}
function filterCustomers(){const status=$('customerStatus').value,plan=$('customerPlan').value;customerTableReplace(state.users.filter(u=>(!status||u.status===status)&&(!plan||u.plan_id===plan)))}
function customerTableReplace(users){$('customerTable').innerHTML=customerTable(users);bindUserButtons()}
function bindUserButtons(){document.querySelectorAll('[data-user-manage]').forEach(b=>b.onclick=()=>openUserModal(state.users.find(u=>u.id===b.dataset.userManage)))}
function customerTable(users){if(!users.length)return'<div class="empty">Müşteri bulunamadı.</div>';return`<div class="table-wrap"><table class="table"><thead><tr><th>Müşteri</th><th>Paket</th><th>Kredi</th><th>Durum</th><th>Kayıt</th><th>İşlem</th></tr></thead><tbody>${users.map(u=>`<tr><td><b>${esc(u.email)}</b><div class="mono">${esc(short(u.auth_subject,32))}</div>${u.wallet_address?`<div class="mono">${esc(short(u.wallet_address,24))}</div>`:''}</td><td>${esc(u.plan_id||'Aktif paket yok')}<div class="small muted">${esc(u.active_entitlement_status||'No active package')}</div></td><td>${num(u.credits)}</td><td>${badge(u.status)}</td><td>${dt(u.created_at)}</td><td><button class="btn small" data-user-manage="${esc(u.id)}">Yönet</button></td></tr>`).join('')}</tbody></table></div>`}
function openUserModal(user){if(!user)return;openModal(`<div class="modal-head"><div><span class="eyebrow">Müşteri yönetimi</span><h2>${esc(user.email)}</h2></div><button class="modal-close" data-close>×</button></div><div class="metadata"><div><label>Durum</label><b>${esc(user.status)}</b></div><div><label>Paket</label><b>${esc(user.plan_id||'Yok')}</b></div><div><label>Kredi</label><b>${num(user.credits)}</b></div><div><label>Paket bitiş</label><b>${dt(user.entitlement_expires_at)}</b></div></div><div class="field"><label>Kredi ekle</label><input class="input" id="creditAmount" type="number" min="1" value="1"><input class="input" id="creditReason" placeholder="İşlem nedeni"></div><div class="modal-actions"><button class="btn" id="addCredit">Kredi ekle</button><button class="btn ${user.status==='banned'?'':'warn'}" id="toggleBan">${user.status==='banned'?'Yasağı kaldır':'Kullanıcıyı yasakla'}</button><button class="btn danger" id="removeUser">Erişimi kaldır</button></div>`);$('addCredit').onclick=async()=>{try{await api('/api/owner/credits/add',{method:'POST',body:JSON.stringify({email:user.email,credits:Number($('creditAmount').value),reason:$('creditReason').value.trim()||'owner_panel'})});closeModal();toast('Kredi eklendi.');loadCustomers()}catch(e){toast(e.message,true)}};$('toggleBan').onclick=async()=>{const ban=user.status!=='banned',reason=prompt(ban?'Yasaklama nedeni:':'Yasağı kaldırma nedeni:','owner_panel');if(reason===null)return;try{await api('/api/owner/users/ban',{method:'POST',body:JSON.stringify({email:user.email,ban,reason})});closeModal();toast(ban?'Kullanıcı yasaklandı.':'Yasak kaldırıldı.');loadCustomers()}catch(e){toast(e.message,true)}};$('removeUser').onclick=async()=>{if(!confirm(`${user.email} erişimini kaldırmak istediğine emin misin?`))return;try{await api('/api/owner/users/remove',{method:'POST',body:JSON.stringify({email:user.email,reason:'owner_panel_removed'})});closeModal();toast('Kullanıcı erişimi kaldırıldı.');loadCustomers()}catch(e){toast(e.message,true)}}}

async function loadRevenue(){const content=$('revenueContent');content.innerHTML='<div class="card loading">Ödeme operasyonları yükleniyor…</div>';try{const [health,requests]=await Promise.all([api('/api/owner/payment-health'),api('/api/owner/payment-requests')]);state.paymentHealth=health;state.payments=requests.payment_requests||[];renderRevenue()}catch(e){content.innerHTML=`<div class="error-box">${esc(e.message)}</div>`}}
function renderRevenue(){const h=state.paymentHealth||{},sum=h.summary||{},pending=state.payments.filter(x=>x.status==='pending');$('revenueContent').innerHTML=`<div class="grid compact-grid">${kpi('Aktif Paddle paket',num(sum.active_entitlements),'Otomatik entitlement','tone-green','◈')}${kpi('Bekleyen manuel ödeme',num(pending.length),'Shopier owner onayı',pending.length?'tone-amber':'tone-green','⌛')}${kpi('Webhook hatası 24s',num(sum.failed_webhook_events_24h),'Paddle olayları',sum.failed_webhook_events_24h?'tone-red':'tone-green','!')}${kpi('Paddle',statusValue(h.paddle),'Production ödeme sağlayıcısı',tone(statusValue(h.paddle))==='bad'?'tone-red':'tone-cyan','₺')}<article class="card span-6"><div class="card-head"><div><span class="eyebrow">Manuel ödeme</span><h2>Shopier onayları</h2></div></div>${paymentTable(state.payments)}</article><article class="card span-6"><div class="card-head"><div><span class="eyebrow">Otomatik ödeme</span><h2>Paddle sipariş akışı</h2></div></div>${orderEvents(sum.latest_payment_events||[])}</article></div>`;document.querySelectorAll('[data-pay-approve]').forEach(b=>b.onclick=()=>reviewPayment(b.dataset.payApprove,true));document.querySelectorAll('[data-pay-reject]').forEach(b=>b.onclick=()=>reviewPayment(b.dataset.payReject,false))}
function paymentTable(items){if(!items.length)return'<div class="empty compact">Ödeme talebi yok.</div>';return`<div class="table-wrap"><table class="table"><thead><tr><th>Müşteri</th><th>Paket</th><th>Tutar</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>${items.map(p=>`<tr><td><b>${esc(p.email)}</b><div class="small muted">${esc(p.full_name)}</div></td><td>${esc(p.product_id)}</td><td>${num(p.amount_try)} ${esc(p.currency)}</td><td>${badge(p.status)}</td><td>${p.status==='pending'?`<button class="btn small primary" data-pay-approve="${esc(p.id)}">Onayla</button> <button class="btn small danger" data-pay-reject="${esc(p.id)}">Reddet</button>`:'—'}</td></tr>`).join('')}</tbody></table></div>`}
function orderEvents(items){if(!items.length)return'<div class="empty compact">Paddle sipariş olayı yok.</div>';return`<div class="table-wrap"><table class="table"><thead><tr><th>Order</th><th>Tutar</th><th>Durum</th><th>Tarih</th></tr></thead><tbody>${items.map(o=>`<tr><td class="mono">${esc(short(o.provider_order_id,30))}</td><td>${money(o.amount_cents)} ${esc(o.currency||'')}</td><td>${badge(o.status)}</td><td>${dt(o.created_at)}</td></tr>`).join('')}</tbody></table></div>`}
async function reviewPayment(id,approve){const reason=prompt(approve?'Onay notu:':'Red nedeni:',approve?'owner_verified_payment':'payment_not_verified');if(reason===null)return;try{await api(approve?'/api/owner/payments/approve':'/api/owner/payments/reject',{method:'POST',body:JSON.stringify({payment_request_id:id,reason})});toast(approve?'Ödeme onaylandı ve paket aktive edildi.':'Ödeme reddedildi.');await refreshDashboard();await loadRevenue()}catch(e){toast(e.message,true)}}

async function loadSecurity(){const content=$('securityContent');content.innerHTML='<div class="card loading">Güvenlik olayları yükleniyor…</div>';try{const d=await api('/api/owner/security-events?limit=200');state.security=d.events||[];renderSecurity()}catch(e){content.innerHTML=`<div class="error-box">${esc(e.message)}</div>`}}
function renderSecurity(){const severities=[...new Set(state.security.map(e=>e.severity).filter(Boolean))];const success=state.security.filter(e=>String(e.event_type||'').includes('success')).length,errors=state.security.filter(e=>tone(e.severity)==='bad').length,paddle=state.security.filter(e=>String(e.event_type||'').includes('paddle')).length;$('securityContent').innerHTML=`<div class="grid compact-grid">${kpi('Başarılı login',num(success),'Owner/customer audit','tone-green','✓')}${kpi('Error event',num(errors),'Kritik + hata','tone-red','!')}${kpi('Paddle olayları',num(paddle),'Ödeme denemeleri',paddle?'tone-amber':'tone-green','₺')}<article class="card span-12"><div class="toolbar"><div><span class="eyebrow">Security audit stream</span><h2>Kim, ne zaman, ne yaptı?</h2></div><div class="filters"><input class="input" id="securitySearch" placeholder="Event, actor, path ara"><select class="select" id="securitySeverity"><option value="">Tüm önem seviyeleri</option>${severities.map(s=>`<option>${esc(s)}</option>`).join('')}</select></div></div><div id="securityTable">${securityTable(state.security)}</div></article></div>`;$('securitySearch').oninput=filterSecurity;$('securitySeverity').onchange=filterSecurity}
function filterSecurity(){const q=$('securitySearch').value.toLowerCase(),severity=$('securitySeverity').value;const items=state.security.filter(e=>(!severity||e.severity===severity)&&(!q||JSON.stringify(e).toLowerCase().includes(q)));$('securityTable').innerHTML=securityTable(items)}
function securityTable(items){if(!items.length)return'<div class="empty">Güvenlik olayı yok.</div>';return`<div class="table-wrap"><table class="table"><thead><tr><th>Zaman</th><th>Olay</th><th>Aktör</th><th>Yol / IP</th><th>Önem</th><th>Metadata</th></tr></thead><tbody>${items.map(e=>`<tr><td>${dt(e.created_at)}</td><td><b>${esc(e.event_type)}</b></td><td>${esc(e.actor_type)}<div class="mono">${esc(short(e.actor_id,28))}</div></td><td class="mono">${esc(e.path||'—')}<br>${esc(e.ip||'')}</td><td>${badge(e.severity)}</td><td class="mono">${esc(short(JSON.stringify(e.metadata||{}),90))}</td></tr>`).join('')}</tbody></table></div>`}

async function loadSystem(){const content=$('systemContent');content.innerHTML='<div class="card loading">Sistem sağlığı yükleniyor…</div>';try{const health=await api('/api/owner/health');renderSystem(health)}catch(e){renderSystem(null,e)}}
function renderSystem(health,error){const d=state.dashboard||{},services=d.services||{};const visible=Object.entries(health?.services||{}).filter(([k,v])=>!['github','openai'].includes(String(k).toLowerCase())&&tone(statusValue(v))!=='bad');$('systemContent').innerHTML=`<div class="grid compact-grid"><article class="card span-8"><div class="card-head"><div><span class="eyebrow">Service matrix</span><h2>Bağımlılıkların gerçek durumu</h2></div></div>${compactServiceGrid(services)}${error?`<div class="error-box" style="margin-top:12px">${esc(error.message)}</div>`:''}</article><article class="card span-4"><div class="card-head"><div><span class="eyebrow">Owner health</span><h2>İç servis kontrolü</h2></div></div><div class="proof-grid vertical">${visible.map(([k,v])=>summaryRow(k,statusValue(v),statusValue(v))).join('')||summaryRow('Durum','Veri yok','warn')}</div></article><article class="card span-12"><div class="card-head"><div><span class="eyebrow">Operasyon ilkeleri</span><h2>Owner güvenlik sınırları</h2></div></div><div class="policy-grid"><div class="success-box">Kanıt yoksa müşteri kartı yok.</div><div class="success-box">Premium erişim entitlement üzerinden açılır.</div><div class="success-box">Hassas değerler owner panelinde gösterilmez.</div></div></article></div>`}

function renderBrain(){const root=$('brainContent');root.innerHTML='<div class="card loading">Owner AI Chat hazırlanıyor…</div>';setTimeout(()=>{if(window.OwnerAIChat?.mount)window.OwnerAIChat.mount(root)},50)}

function openModal(html){$('modalRoot').innerHTML=`<div class="modal-backdrop"><section class="modal">${html}</section></div>`;document.querySelectorAll('[data-close]').forEach(b=>b.onclick=closeModal);document.querySelector('.modal-backdrop').onclick=e=>{if(e.target.classList.contains('modal-backdrop'))closeModal()}}
function closeModal(){$('modalRoot').innerHTML=''}

async function refreshDashboard(){if(state.loading)return;state.loading=true;setSync('Güncelleniyor','warn');try{state.dashboard=await api('/api/owner/command-center');showApp();setSync(`Canlı · ${dt(state.dashboard.generated_at)}`,'ok');renderNavigation();if(state.active==='command')renderCommand();if(state.active==='arvis')renderArvis();if(state.active==='system')renderSystem(null)}catch(e){if(e.status===404||e.status===401){showLogin('Owner oturumu gerekli.')}else{setSync('Bağlantı hatası','bad');toast(e.message,true)}}finally{state.loading=false}}
async function loadPage(id){if(id==='command'){renderCommand();return}if(id==='arvis'){await loadArvis();return}if(id==='customers'){await loadCustomers();return}if(id==='revenue'){await loadRevenue();return}if(id==='security'){await loadSecurity();return}if(id==='system'){await loadSystem();return}if(id==='brain'){renderBrain()}}

$('loginForm').onsubmit=async e=>{e.preventDefault();$('loginError').classList.add('hidden');$('loginButton').disabled=true;try{await api('/api/owner/login',{method:'POST',body:JSON.stringify({wallet:$('loginWallet').value.trim(),secret:$('loginSecret').value})});$('loginSecret').value='';await refreshDashboard();await showPage('command')}catch(err){showLogin(err.status===404?'Owner bilgileri doğrulanamadı.':err.message)}finally{$('loginButton').disabled=false}};
$('logoutButton').onclick=async()=>{try{await api('/api/owner/logout',{method:'POST'});}catch{}state.dashboard=null;showLogin('Oturum kapatıldı.')};
$('refreshButton').onclick=async()=>{await refreshDashboard();await loadPage(state.active)};

renderNavigation();refreshDashboard();setInterval(()=>{if(!$('appView').classList.contains('hidden'))refreshDashboard()},30000);
})();

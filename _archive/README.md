# Archive

This directory contains legacy code moved out of active deploy paths.

- `apps-web-next`: archived Next.js app (not deployed in current runtime).
- `koschei-backend-old`: archived duplicate backend (not canonical for this phase).

These are retained for reference only and should not be used for active production routing.
